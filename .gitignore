.DS_Store
/original_materials/
/original-materials/
/templates/

# Raw AWS source decks — kept local because of their SIZE and FILE COUNT (1GB+, many files), not licensing.
# Cited by module/slide in slide_plan.md. The assembled, curated Topic decks (topic_NN/Topic_NN_Slides.pptx)
# ARE tracked — they're small, and the repo is the instructor-to-instructor channel for finished materials.
**/source_slides/

# Python bytecode
__pycache__/
*.pyc

# Office open-file locks (created while a .docx/.xlsx is open in Word/Excel)
~$*

.venv/
