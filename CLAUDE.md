# diploma-cloud-cyber-content — Working Notes

> **Project-wide working rules and cross-repo context live in the umbrella repo's
> `CLAUDE.md`** (loaded automatically when working from the umbrella root — including the
> standing rules on presumptive planning, TBD markers, and no unilateral git operations).
> This file holds only **content-specific** instructions for cluster/UoC assessment work.

This is the **curriculum / assessment authoring** repo (clusters, UoC mappings, validators). Its
process and conventions are documented in the umbrella `docs/`, and the per-cluster working state is in
the umbrella's LLM memory — both reached via the umbrella `CLAUDE.md` (always loaded before this file),
so this file carries no separate pointers.

<!-- Content-specific instructions not already covered by the umbrella docs/memory go here. -->
