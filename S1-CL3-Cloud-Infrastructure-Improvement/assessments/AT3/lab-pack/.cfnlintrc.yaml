templates:
  - baseline.yaml
  - improved.yaml
ignore_checks:
  # W1011: the DB master password is a NoEcho parameter passed at deploy time (not stored). The
  # lab deliberately supplies the secret at deploy, not via Secrets Manager, to keep the baseline
  # deployable in AWS Academy without creating IAM. Hardening to Secrets Manager is an AT-design topic.
  - W1011
